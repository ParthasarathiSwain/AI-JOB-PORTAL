server:
  port: 8989

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/aijobportal?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: TURGUT
    password: TURGUT
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  ai:
    openai:
      api-key: "your Key"

logging:
  level:
    root: INFO
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG

servlet:
  multipart:
    max-file-size: 10MB
    max-request-size: 20MB

# File storage for resumes & PDFs
file:
  upload-dir: ./uploads/resumes
  max-size: 10MB
